<h1>Are you sure to cancel the subscription?</h1>
<p>
  You still can use Sail by <%= @issue.user.local_time_expr(Time.parse(@issue.agreement.billing_info[:last_payment][:time])+ 1.month - 1.second , :long)  %>.
</p>

<%= form_tag "/billing/students/issues/paypal/cancel/#{@student.auth_token}/#{@issue.id}", id: "payment-form", method: "post" do %>
  <button class="sc-bdVaJa eEjsbL" data-active="true" font-size="32" height="88" type="blue" width="494">Cancel</button>
<% end %>
